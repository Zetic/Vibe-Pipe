# Adding projects

Vibe Pipe is data-driven. Normal catalogue updates should only require changes to `projects.json` and `assets/projects/`.

Keep project banners near an 8:3 aspect ratio. The site will crop other ratios with `object-fit: cover`.

A project entry requires `name`, `url`, and `image`. See `projects.example.json` for a complete example.
