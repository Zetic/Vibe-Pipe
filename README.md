# Vibe Pipe

Static project gallery for Zetic projects, designed for GitHub Pages.

Implementation is developed through pull requests. See `projects.json` after the initial site PR is merged to add projects without editing the HTML, CSS, or JavaScript.
